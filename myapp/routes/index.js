var express = require('express');
var router = express.Router();

/* GET home page. */
//router.get('/', function(req, res, next) {
//  let todaysDate = format(new Date(), 'dd/mm/yy');
//  res.render('index', { title: 'Today\'s Date:', date: todaysDate });
//});
//
//module.exports = router;
